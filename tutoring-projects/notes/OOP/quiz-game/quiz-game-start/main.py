#
from data import question_data
#
question_bank = [] # an array
for i in range(len(question_data)):
    q_text = question_data[i]["text"] # this is our current question
    q_answer = question_data[i]["answer"]





